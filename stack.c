#include "monty.h"

/**
 * st_stack - for the top
 * @head: head of stack not needed
 * @count: line num- same
 * Return: nain
 */
void st_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.change = 0;
}
